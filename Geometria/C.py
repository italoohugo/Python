print('Programa Calculo da area de um Trapezio')

Base = float(input('Digite a base Maior: '))
base = float(input('Digite a base Menor: '))

h = float(input('Qual a autura: '))

area = (Base + base) * h / 2

print('A area de um Trapezio é:',area)